import React from "react";
import ToolCard from "../../ToolCard";
import { Border } from "@/components/svgs";

const BorderSelector = () => {
  return <ToolCard text='Border' icon={<Border />} />;
};

export default BorderSelector;
